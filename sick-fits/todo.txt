// TODO

BACKEND
1. datamodel.prisma
  _current:
  permissions: [Permission]
  _must be
  permissions: [Permission]!

  
FRONTEND
1. Header.js
	NProgress  
	progress bar not working
	
2. PleaseSignIn для каждой страницы, где требуется логин

const user = await db.query.user(
    {
      where: { id: req.userId },
    },
    'id, email, name, permissions'
  );


